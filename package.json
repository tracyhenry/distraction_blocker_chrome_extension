{
  "name": "focus-mode-extension",
  "version": "1.0.0",
  "description": "A Chrome extension that blocks distracting websites during focus mode",
  "scripts": {
    "build": "npm run build:icons && npm run build:css",
    "build:css": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "build:icons": "node scripts/generate-icons.js",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "sharp": "^0.33.2",
    "tailwindcss": "^3.4.1"
  }
}

